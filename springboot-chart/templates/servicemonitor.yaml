apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor

metadata:
name: springboot-monitor
labels:
release: prometheus

spec:
selector:
matchLabels:
app.kubernetes.io/name: springboot-chart

endpoints:

* port: http
  path: /actuator/prometheus
  interval: 15s
